# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
/out/
/build/
next-env.d.ts
tsconfig.tsbuildinfo

# private node copy the launcher fetches when the machine has none
.runtime/

# runtime — leading slash so these only match the dirs at the repo root, and not
# route folders that happen to share the name (app/api/admin/cache/)
.env
.env.local
/cache/
/data/
*.log

# reference material (not part of the project)
_reference/

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/
