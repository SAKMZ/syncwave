/**
 * Fixed facts about the project and the studio behind it.
 *
 * Here rather than inline so a fork changes one file, and so the repo URL
 * can't drift between the landing page, the footer and the admin console.
 */
export const REPO_URL = "https://github.com/SAKMZ/syncwave";
export const STUDIO = {
  name: "AWETOMIQ",
  email: "hello@awetomiq.com",
  url: "https://awetomiq.com",
} as const;
